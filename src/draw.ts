
import * as d3 from "d3"
import {data} from "./data"
import { createEllipse, createLine, createRect, createText } from "./shapes"

const frame = figma.currentPage.selection[0] 


export function draw() {
    if(frame.type == "FRAME") {

        data.forEach( (d:any, i:number) => {
            
          
    
        })
    }
}